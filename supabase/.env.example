# Values substituted into supabase/config.toml env(...) references by the Supabase CLI.
# Copy to supabase/.env (git-ignored) before running `npx supabase config push`.

# Resend API key, used as the SMTP password for Supabase Auth emails
RESEND_API_KEY=re_...
